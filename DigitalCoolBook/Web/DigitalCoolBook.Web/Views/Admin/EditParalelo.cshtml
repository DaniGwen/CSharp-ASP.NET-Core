@model DigitalCoolBook.App.Models.GradeParaleloViewModels.ParaleloCreateViewModel

@{
    ViewData["Title"] = "EditClass";
    Layout = "_Layout";
}

<div class="offset-md-3 col-md-6 offset-sm-0 col-sm-12 mt-3">
    <div class="page-wrapper">
        <h3>Reassign teacher to class</h3>
        <hr />
        <form asp-action="EditParalelo" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" name="GradeParaleloId" id="GradeParaleloId" value="@Model.GradeParaleloId" />
            <div class="form-group mt-2">
                <select class="form-select select" name="IdTeacher">
                    <option selected>Select Teacher...</option>
                    @foreach (var teacher in Model.Teachers)
                    {
                        <option value="@teacher.Id">@teacher.Name</option>
                    }
                </select>
            </div>
            <div class="form-group mt-2">
                <select class="form-select select" name="IdGrade">
                    <option selected>Select Class...</option>
                    @foreach (var grade in Model.Grades)
                    {
                        <option value="@grade.GradeId">@grade.Name</option>
                    }
                </select>
            </div>
        </form>
    </div>
    <div class="buttons-wrapper">
        <a asp-controller="Admin" asp-action="EditParalelos"><i class="fas fa-lg fa-arrow-circle-left"></i></a>
        <input class="btn btn-success btn-sm" type="submit" value="Save" />
    </div>
</div>


