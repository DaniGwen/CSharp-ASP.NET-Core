@model IEnumerable<DigitalCoolBook.App.Models.GradeParaleloViewModels.ParaleloViewModel>

@{
    ViewData["Title"] = "CreateParalelo";
    Layout = "_LayoutMenus";
}

<h2>Class control panel</h2>
<p>
    <a class="btn btn-secondary" href="/Admin/CreateParalelo">Create new class</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Teacher name
            </th>
            <th>
                Class name
            </th>
            <th>
                Students count
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.TeacherName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GradeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Students.Count)
                </td>
                <td>
                    <button class="text-white btn btn-secondary btn-sm">
                        @Html.ActionLink("Edit", "EditParalelo", new { id = item.Id })
                    </button>
                    <button class="text-white btn btn-secondary btn-sm">
                        @Html.ActionLink("Delete", "DeleteParalelo", new { id = item.Id })
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>
